# powershell: install docker-desktop
# powershell: install wsl 2
# powershell: install wsl kernel updates
# powershell: install 
# powershell: install microsoft-terminal
# powershell: wsl --set-default-version 2
# powershell: install wsl distro (appx file)
# powershell: wsl -s Ubuntu*
# microsoft-terminal: add nameservers (8.8.8.8, 8.8.4.4): sudo vi /etc/resolv.conf
# microsoft-terminal: sudo apt-get update && sudo apt-get install -y jq
# microsoft-terminal: sudo sh -c "$(curl -fsSL https://raw.githubusercontent.com/gkoehl/public/master/scripts/workstation/init.cmd)"
# microsoft-terminal: y for default shell to zsh? (need to exit subshell)